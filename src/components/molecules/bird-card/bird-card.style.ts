import styled from "@emotion/styled";

const StyledImageContainer = styled.div`
  position: relative;
`;

const StyledLoadingOverlay = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;

export { StyledImageContainer, StyledLoadingOverlay };
